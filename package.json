{
  "name": "seedora",
  "version": "1.0.0",
  "private": true,
  "description": "Seedora premium Indian dry fruits, laddus, spices, and gifts storefront with local backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js"
  },
  "engines": {
    "node": ">=20"
  }
}
